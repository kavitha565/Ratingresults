import { QuotesHeadingPipe } from './quotes-heading.pipe';

describe('QuotesHeadingPipe', () => {
  it('create an instance', () => {
    const pipe = new QuotesHeadingPipe();
    expect(pipe).toBeTruthy();
  });
});
